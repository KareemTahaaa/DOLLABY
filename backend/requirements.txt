fastapi
uvicorn
motor
pymongo
python-dotenv
python-multipart
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
pydantic
pydantic-settings
openai
httpx
aiofiles
groq
replicate
gradio_client
# ── Virtual Try-On: Simplified Model (only needed for local training) ────────
# Uncomment these to train locally on GPU
# torch>=2.0.0
# torchvision>=0.15.0

# ── Virtual Try-On: HR-VITON (only needed if VITON_MODE=local) ───────────────
# torch
# torchvision
# mediapipe
# scipy
Pillow
tqdm
